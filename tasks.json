{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rust: Build",
            "type": "shell",
            "command": "cargo build --manifest-path ${workspaceFolder}/Cargo.toml"
        }
    ]
}